<%= form_for(@planet) do |f| %>
  <% if @planet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@planet.errors.count, "error") %> prohibited this planet from being saved:</h2>

      <ul>
      <% @planet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :star %><br />
    <%= collection_select(:planet, :star_id, Star.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :asteroid_belt_count %><br />
    <%= f.number_field :asteroid_belt_count %>
  </div>
  <div class="field">
    <%= f.label :poco_owner_id %><br />
    <%= f.number_field :poco_owner_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
